# csrc_patches

Diffs vs upstream `vllm-project/vllm` `csrc/` for each ported kernel. Format: unified diff against the pinned upstream SHA (recorded in `docs/03_BUILD.md`).

These diffs are applied by `scripts/build_vllm_dsa_sm86.sh` during the build.

— Authored by ProtoAI-Bakari, with Assistance by Claude Opus 4.7 (claude-opus-4-7) [1M ctx, max effort]
